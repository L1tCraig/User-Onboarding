import React from 'react'

export default function Form(data) {
    const { values, update, submit } = data

   return (
       <form>
           <div>
               Start
           </div>
       </form>
   )
}